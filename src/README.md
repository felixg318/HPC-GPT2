# Process Simplified
* Pass raw text in first arguement
* Raw text is passed to Tokenizor object that extracts tokens and holds important info about the text
* Create token embeddings and pass this layer to the input tokens to create a weight matrix of input tokens
